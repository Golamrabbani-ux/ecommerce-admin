import React from 'react';
import Layout from '../../components/Layout/Layout';
import '../containers.css';

const Home = () => {
    return (
        <Layout sidebar>
            
        </Layout>
    );
};

export default Home;